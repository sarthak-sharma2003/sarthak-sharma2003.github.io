import { defineConfig } from 'astro/config';

// Deployed as a GitHub Pages *user site*: the repo must be named
// sarthak-sharma2003.github.io — then no `base` path is needed.
export default defineConfig({
  site: 'https://sarthak-sharma2003.github.io',
});
